# Сверка выбранного оформления

**Источник:** `client/public/graphics/selected-design.png` — выбранный пользователем второй макет, исходный файл `C:/Users/test/.codex/generated_images/01a0d88c-56a8-7ac1-8ee6-f9dfe4e5ed4e/exec-1e9a89d6-125b-4600-89e4-18b24d21a29b.png`.

**Результат:** `docs/evidence/game-desktop.png`, работающий интерфейс `http://127.0.0.1:8180`. Снимок получен из идентичной сборки в отдельном приложении для проверки на 8181. Обе картинки открыты вместе в одном сравнении после исправлений.

Размер источника и снимка: 1487 × 1058 пикселей, окно 1487 × 1058 CSS px, плотность 1. Масштабирование для сравнения не применялось. На снимке — начало первой учебной смены. В макете уже нарисован багаж; после уточнения пользователя он появляется только через 20 секунд после первого ответа. Поэтому раннее отсутствие второй карточки и срока — ожидаемое отличие состояния.

## Найденное и исправленное

1. [P2, исправлено] На большом экране исходный портрет 131 × 238 растягивался и размывался; отсутствовал знак рядом с названием. Использованы реальные фрагменты выбранного макета. Изображение не перерисовано кодом.
2. [P2, исправлено] При первом смещении кадра обрезалось лицо. Кадр сдвинут; ширина ограничена, край плавно скрывается. Повторный снимок показывает лицо целиком.
3. [P2, исправлено] На ширине 768 в кадр попадал текст из изображения макета. Максимальная ширина иллюстрации на малом экране ограничена 200 px. Повторно просмотрен `docs/evidence/width-768.png`: постороннего текста нет.
4. [P2, исправлено] Окно подготовки не удерживало фокус клавиатуры. Проверены начальный фокус, Shift+Tab, Escape и возврат к кнопке открытия.

## Проверенные свойства

- **Шрифт:** локальный Arial/Helvetica без зависимости от сети. Семейство в растровом макете не определено; геометрия букв немного отличается. Размеры, контраст, переносы реплик и кнопок проверены. Иерархия заголовка, реплики и действий сохранена.
- **Расположение:** верхняя панель 82 px, узкая навигация, диалог слева, обращения и показатели справа. Между карточками и показателями добавлен отступ. На телефоне обращения и срок идут перед диалогом; элементы не уходят за горизонтальную границу экрана.
- **Цвет:** светлый фон, тёмно-синий текст, синий выбор и фокус, зелёная лояльность. Правильный ответ заранее не выделяется. Синяя рамка на снимке относится к положению указателя, а не оценке ответа.
- **Изображения:** знак и портрет взяты из выбранного макета; салон и аватары — из предоставленного набора. Изображение салона намеренно показывает стандартный класс. В исходных маленьких аватарах детализация ограничена размером файла, это допустимо при отображении 43 px.
- **Текст:** формулировки проводника и пассажира взяты из сценариев. «Ваше действие», «Пауза», «Продолжить», «Что получилось, а что стоит повторить». Термины программы не попадают в основной путь игры. Необязательные рекламные подписи макета не перенесены.

Полноразмерное совместное сравнение позволяет прочитать кнопки, реплики, подписи шкал и заголовки без отдельного увеличения. Дополнительные обрезки этих областей не требовались. Малые экраны отдельно просмотрены целиком: 390 × 900, 768 × 900, 1366 × 900; длинная страница захвачена полностью.

## Проверки поведения

Пройдены обе игровые цепочки, пауза и возвращение, семь автоматических браузерных проверок. В основном успешном пути ошибок JavaScript нет. Потеря связи показывает русское сообщение и не сбрасывает прохождение. Файлы подтверждения находятся в `docs/evidence/` и перечислены в `docs/acceptance.md`.

## Допустимые отличия и дальнейшая отделка

[P3] Точное семейство шрифта и его начертание можно согласовать с заказчиком при передаче фирменных файлов. Сейчас используется доступный локальный шрифт. Номер места, вагона и рекламная подпись из макета не перенесены как неподтверждённые детали. В первой сцене срок показывается только после возникновения задачи.

Не осталось открытых замечаний P0/P1/P2. Проверены шрифт, расположение, цвета, изображения, тексты и малые экраны.

final result: passed
