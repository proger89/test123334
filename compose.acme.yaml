# Only used for the first certificate, while the application still serves HTTP.
services:
  web:
    volumes:
      - ./infra/nginx.acme.conf:/etc/nginx/conf.d/default.conf:ro
      - ./runtime/acme:/var/www/acme:ro
