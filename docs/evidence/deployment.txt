Проверка развёртывания на Aeza — 25 сентября 2026 года.

Адрес: http://185.173.147.205/
Репозиторий: https://github.com/proger89/test123334
Каталог: /opt/vsm-hackathon
Compose-проект: vsm-hackathon-demo
Собранный код: 989e018a64c092c001c409712950bff36ee78a20.
Последующие коммиты документации не меняют исходники приложения или настройки его сборки.

Ubuntu 24.04.1, Docker 29.1.3, Compose 2.40.3.
Приложение собрано на сервере из клонированного репозитория.
Первая загрузка PHP-образа остановилась на одном слое; повторная сборка успешно догрузила тот же закреплённый образ из Docker Hub.
Подготовленный запасной перенос образа не использован; временный файл на сервере удалён.

Миграции выполнены, init завершился с кодом 0.
api, worker, web, db — healthy.
GET /api/health/ready извне: 200, {"ready":true}.
База не публикуется; наружу открыт только порт web 80.
Используется отдельный постоянный том базы. Секреты созданы на сервере и исключены из Git.

Два автоматических браузерных прохода через публичный адрес — пройдены:
1. Ошибка → упражнение → пауза и обновление → сохранение результата → самостоятельная проверка.
2. Две успешные проверки → испытание → достижения → три рейтинга → сохранение после обновления.
Отчёт: aeza-browser-tests.txt. Ошибок JavaScript в проверенных цепочках нет.
Во встроенном браузере дополнительно пройдены критическая ошибка полной смены, реальная просрочка упражнения и успешный повтор. В консоли нет предупреждений или ошибок. Снимок успешного упражнения на публичном стенде: aeza-practice-result.png.
Первый полезный экран: 1713 мс в одном измерении с компьютера разработчика; это не статистика доступности или нагрузки.

Память после запуска (один снимок): api 50.17 MiB, worker 34.17 MiB, web 5.87 MiB, db 41.77 MiB.
На диске осталось 9.7 GB. Другие приложения и их тома не изменялись.

Публичное демо работает по HTTP; HTTPS и постоянная учётная запись не настроены.
Оценки учебные, предметное согласование заказчиком не заявляется.
